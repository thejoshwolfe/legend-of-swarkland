{
  textures: {
    spritesheet: {
      allowRotate90: false, // SDL2 textures don't let you override the vertex array
      maxWidth: 512,
      maxHeight: 512,
      globImages: [
        {
          path: "assets/",
          glob: "*",
          prefix: "img/",
        },
      ],
    }
  }
}
